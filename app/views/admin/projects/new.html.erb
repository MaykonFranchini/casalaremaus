<div class="container my-5">
  <div class="row justify-content-center">
    <div class="col-sm-12 col-md-8">
      <h2>Inserir um novo Projeto</h2>
      <p>Preencha os campos abaixo para postar um novo projeto no website da Casa Lar Emaus</p>
    </div>
  </div>
  <div class="row justify-content-center">
    <div class="col-sm-12 col-md-8">
      <div class="form-volunteer-box py-5">
        <%= simple_form_for [:admin, @project] do |f| %>
          <%= f.input :name, label: 'Qual o nome do projeto?', class: "form-group" %>
          <%= f.input :description, label: 'Descrição do projeto', class: "form-group" %>
          <%= f.input :photo, as: :file, label: 'Foto para ilustrar o projeto' %>
          <%= f.input :status, label: 'Esse projeto está ativo?', class: "form-group" %>
          <%= f.button :submit, "Criar novo projeto", class: "button-banner" %>
        <% end %>
      </div>
    </div>
  </div>
</div>


